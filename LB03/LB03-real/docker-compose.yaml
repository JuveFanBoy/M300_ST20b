version: '3'
services:
  vaultwarden:
    image: vaultwarden/server:latest
    container_name: vaultwarden
    restart: always
    ports:
      - 8080:80
    environment:
      - ROCKET_PORT=80
      - ROCKET_WORKERS=10
    volumes:
      - ./data:/data
